import { OpenApi } from '../open-api/open-api';

/**
 * Generate the open api document
 *
 * @see Builder::generate
 */
export function generate(api: OpenApi): OpenApi {
    // TODO handle validation logic too
    return api;
}
