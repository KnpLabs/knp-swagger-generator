import { Violation } from '../violation';
import { OpenApi } from '../open-api/open-api';

/**
 * Validate an open api document
 *
 * @see Builder::validate
 */
export function validate(api: OpenApi): Violation[] {
    // TODO handle validation
    return [];
}
